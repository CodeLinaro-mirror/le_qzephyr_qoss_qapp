.. zephyr:code-sample:: buttons
   :name: Buttons
   :relevant-api: gpio_interface

   Handle GPIO inputs with interrupts.

Overview
********

A simple buttons demo showcasing the use of two GPIO input with interrupts.
The sample prints a message to the console each time a button is pressed
with appropriate info which one was it.
